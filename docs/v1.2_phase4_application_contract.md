# 匠星智造 MES V1.2 — Phase 4 应用层契约（数据库 ↔ 服务边界）

| 项 | 值 |
|---|---|
| 文档版本 | V1.2（V1.1 + 发运域裁决冻结：新增第八章发运域契约、shipment_pallet 实体、M-P4-2/D-P4-2 交付项） |
| 日期 | 2026-09-19 |
| 上游冻结基线 | 《匠星智造 MES V1.2 最终架构裁决与整改清单（裁决稿）》、`docs/v1.2_database_domain_model.md`（130 实体/102 关系/34 约束）、`docs/database_design_v1.2.md` V1.1（131 物理表）、`docs/v1.2_database_design_final_closure.md`（NB-1~NB-8） |
| Phase 3 基准 commit | `30e406b5149c63b28aaecfc370dac0f30178b59c`（jiangxing-mes，131 表/216 FK/291 约束/T-1~T-16/53 用例全绿） |
| Phase 3 Gate 结论 | **B — 数据库不修改，应用层责任以本契约为准强制固化** |
| 文档效力 | 本文档是 Phase 4 API/service 层的**强制验收依据**。凡与本文冲突的实现按本文整改；V1.0/V1.1 不新增任何实体/业务状态/业务关系；**V1.2 唯一例外**：经 2026-09-19 发运域裁决新增 `ship.shipment_pallet` 实体与 `ship.shipment.container_id` 列（见第八章与 `docs/v1.2_shipping_business_decisions.md`），其余冻结规则不变 |

---

## 0. 边界总纲

Phase 3 数据库按冻结设计将以下职责**划归应用层**（这是既定分工，不是缺口）：

1. 事务编排——"业务事实 + stock_ledger + stock_balance"三段原子性；
2. 状态机——ENUM 只保证"值合法"，**不保证"状态顺序合法"**；
3. 幂等入口——部分动作有 DB 幂等键，部分动作幂等判据由 service 层实现；
4. 冲正守卫——`aud.reversal_record` 无 DB 唯一约束（NB-P3-1），防重复依赖 service。

本文将上述责任正式固化为可验收契约。

**账务符号语义（全程适用，来源：`database_design_v1.2.md` 7.5 行 600 约定）**：

- `stock_ledger.qty_weight` 一律存**正幅值**，资金方向由 `movement_type` 表达；
- 守恒公式：入账方向（`stock_in`/`return`/`surplus_in`/`count_gain`）取 `+qty_weight`，出账方向（`issue`/`surplus_issue`/`count_loss`）取 `-qty_weight`；
- **唯一例外**：冲正行（`correction_of_id` 非空）`qty_weight` 存**反号**（原行 +30 → 冲正行 -30），与原行同 `movement_type`，凭 `correction_of_id` 参与同一守恒公式；
- 任何时刻：`stock_balance.weight_kg` == 该 (batch, location) 全部账行按守恒公式合计（H-3 已验证）。

---

## 一、库存事务契约（7 类动作）

### 1.0 统一事务模板（7 类动作强制遵守）

```
BEGIN（单事务，禁止中间提交）
  ① 锁定：对本次事务将触及的 whs.stock_balance 行
     SELECT ... FOR UPDATE，按 material_batch_id 升序逐行加锁
     （多批次动作必须升序，防止交叉死锁）
  ② 业务事实写入（各动作对应事实表，见 1.1~1.7）
  ③ stock_ledger INSERT（符号语义见第 0 节）
  ④ stock_balance UPDATE（原子算式：
     SET weight_kg = weight_kg ± w / pieces = pieces ± n，
     禁止先读后写覆盖式赋值；受 CHECK weight_kg >= 0 兜底）
COMMIT
```

**强制规则**：

- R-TX-1：②③④ 必须处于**同一个数据库事务**，任何一步失败整体 ROLLBACK，业务事实不得残留；
- R-TX-2：**特别禁止**"先写业务事实提交，再写库存流水"、"先改余额提交，之后补流水"等任何跨事务编排；
- R-TX-3：禁止应用层把 `stock_balance.weight_kg` 读到内存计算后整值回写（丢失并发更新）；只允许原子增量算式；
- R-TX-4：事务内出现 SQL 异常（含 CHECK 违例）必须 ROLLBACK，**禁止捕获后继续用同事务写其他数据**；
- R-TX-5：service 层不得以任何方式绕过 T-9/T-10/T-11/T-16 触发器保护（含伪造 GUC：`mes.issue_posting`/`mes.reversal_context` 仅允许在对应过账/冲正流程内设置）。

### 1.1 入库（成品上架 / 材料验收上架）

| 要素 | 契约 |
|---|---|
| 业务事实 | 成品：`prod.finished_goods_stock`（status='in_stock'）+ `prod.finished_goods_event('stock_in')`；材料：验收合格行生成 `whs.material_batch`（**不产生任何库存事实**，H-1 已验证） |
| stock_ledger | `movement_type='stock_in'`，qty_weight=+w 正幅值 |
| stock_balance | `weight_kg + w`（UPSERT，键=(material_batch_id, storage_location_id)） |
| 事务 | ①~④ 同事务；成品侧 `finished_goods_stock` INSERT 与其 ledger 行同事务 |
| 行锁顺序 | 先锁目标 balance 行；成品上架另受 `uq_finished_goods_stock_in_stock`（在库 partial unique）天然排他 |
| 失败回滚 | 整体回滚 |
| 重复请求 | 分批上架为**合法业务**（同批次同库位可多笔 stock_in），禁止以"已有非零在库"判重（V1.0 原表述勘误）。幂等锚=`stock_ledger.client_token`（裁决 Q3=A，partial unique WHERE NOT NULL，由迁移 **M-P4-1** 落地，见第七章交付清单）：service 先 SELECT by client_token，命中返回首次上架结果；成品上架另以 `uq_finished_goods_stock_in_stock` 在库唯一双保险 |
| 幂等返回 | 重复请求返回**首次上架结果**（同一 stock_in 账行/在库行引用），不产生第二笔业务事实 |

### 1.2 领料/出库

| 要素 | 契约 |
|---|---|
| 业务事实 | `whs.material_issue_document`（issue_no UNIQUE）+ `whs.material_issue_line`（单据内 line_no 唯一：`uq_material_issue_line_doc_line_no`） |
| stock_ledger | `movement_type='issue'`，`issue_line_id` 必填关联明细行，qty_weight=+w 正幅值 |
| stock_balance | `weight_kg - w` |
| 事务 | 单据+行+账行+issued_qty 刷新+余额扣减**五步同事务** |
| 行锁顺序 | balance 行升序锁 → INSERT 单据/行 → INSERT ledger → UPDATE issued_qty（T-11）→ UPDATE balance |
| T-11 义务 | `issued_qty` 刷新**必须**执行且在账行之后（同一事务）——T-11 强制账行幅值合计 == issued_qty（容差 0.001）；**禁止留空 issued_qty 绕过聚合校验** |
| 失败回滚 | 整体回滚 |
| 重复请求 | 以 issue_no 为幂等判据：service 先 SELECT by issue_no，命中即返回既有单据；并发兜底=UNIQUE 拦截后转幂等返回（见第四章 R-ID-3） |
| 幂等返回 | 第二次请求返回**首次创建的单据**及其过账结果，不产生第二张单据/第二笔账 |

### 1.3 直接消耗

| 要素 | 契约 |
|---|---|
| 业务事实 | `whs.material_consumption`（source_kind/source 批次或余料二选一，CHECK 强制） |
| stock_ledger | **两种既定模式**（冻结设计 7.9，防双重扣账）：A 领料出库模式——消耗不另出账，出库即 1.2 的 issue 行；B 直接消耗模式——账行 `movement_type='issue'` + `consumption_id` 关联。同一消耗事实**至多一条**原始出账路径 |
| stock_balance | 模式 B 中 `weight_kg - w` 与账行同事务；模式 A 由 1.2 承担 |
| 事务 | 事实+账行（模式 B）+余额同事务 |
| 行锁顺序 | balance 行升序锁 → INSERT consumption → INSERT ledger(consumption_id) → UPDATE balance |
| 失败回滚 | 整体回滚 |
| 重复请求 | **DB 级幂等键**：`client_token`（partial unique WHERE NOT NULL）——service 先 SELECT by client_token，命中返回既有消耗 |
| 幂等返回 | 同 token 重试返回**首次消耗事实**，绝不能落第二行 |

### 1.4 退料

| 要素 | 契约 |
|---|---|
| 业务事实 | `whs.stock_ledger(movement_type='return', return_reason_id=...)`——退料是**独立 movement_type**，**严禁用负数领料行冒充退料** |
| stock_ledger | `return` 行，qty_weight=+w 正幅值 |
| stock_balance | `weight_kg + w` |
| 事务 | 账行+余额同事务（退料无独立单据表，账行即事实） |
| 行锁顺序 | balance 行升序锁 → INSERT ledger('return') → UPDATE balance |
| 失败回滚 | 整体回滚 |
| 重复请求 | 幂等锚=`stock_ledger.client_token`（Q3=A，M-P4-1）：service 先 SELECT by client_token，命中返回首次结果；同批次多次退料是合法业务，禁止以既有 return 行判重 |
| 幂等返回 | 重复请求返回首次退料账行引用，不产生第二笔 return |

### 1.5 余料（入池 / 出池）

| 要素 | 契约 |
|---|---|
| 业务事实 | `whs.surplus_material`（R27 血缘：source_batch_id 必填；parent_surplus_id 支持父子且 CHECK 禁自父；status: in_pool→issued/consumed/scrapped） |
| stock_ledger | 入池：`surplus_in` + `surplus_id`（CHECK 强制非空）；余料出库：`surplus_issue` + `surplus_id`（同样强制） |
| stock_balance | 入池 `+w`；出池 `-w`（出库自哪个批次-库位由余料当前所在 balance 行定） |
| 事务 | 余料事实+账行+余额同事务 |
| 行锁顺序 | balance 行升序锁 → INSERT/UPDATE surplus → INSERT ledger → UPDATE balance |
| 失败回滚 | 整体回滚 |
| 重复请求 | 入池幂等锚=`stock_ledger.client_token`（Q3=A，M-P4-1）；`source_cutting_line_id` 仅作追溯引用不作判重依据（一行切割可产生多块余料，多次入池合法）；status 流转（in_pool→issued）由 service 校验，禁止从 issued 跳回 in_pool |
| 幂等返回 | 重复入池返回首次余料 id，不产生第二条 surplus/第二笔 surplus_in |

### 1.6 报废

| 要素 | 契约 |
|---|---|
| 业务事实 | 构件报废：`prod.component_scrap_record`（scrap_no UNIQUE；一构件至多一次生效报废 partial unique WHERE is_effective）+ 构件状态 `production_status='scrapped'`；材料/余料报废：`whs.scrap_record`（source_type ∈ {consumption, surplus}，CHECK 强制） |
| stock_ledger | 构件报废**不产生材料库存账**（构件非库存物）；余料报废：先出池（`surplus_issue` 账行 + balance 扣减 + surplus.status→'scrapped'），`scrap_record` 与出池账行**同事务** |
| stock_balance | 仅余料报废路径扣减（经 surplus_issue） |
| 事务 | 报废事实 + （余料路径：出池账行 + 余额）同事务 |
| 行锁顺序 | balance 行升序锁 → INSERT 报废事实 →（余料路径）INSERT ledger('surplus_issue') → UPDATE surplus.status → UPDATE balance |
| 失败回滚 | 整体回滚 |
| 重复请求 | 构件报废：scrap_no 查重 + 生效报废 partial unique 双保险；材料/余料报废：scrap_no（客户端生成，Q4=A）为幂等令牌，出池账行另受 `stock_ledger.client_token`（M-P4-1）保护 |
| 幂等返回 | 重复请求返回首次报废事实（同 scrap_no / 同引用），不产生第二次报废 |

### 1.7 冲正

**独立契约，见第二章**（守卫模式，不适用 1.0 模板①的 balance 先锁顺序——必须**先锁原记录**）。

---

## 二、冲正契约（F-4 守卫模式正式固化）

### 2.1 守卫流程（唯一合法冲正入口）

```
BEGIN（单事务）
  ① SELECT set_config('mes.reversal_context','on',true)          -- T-10 要求
  ② SELECT id FROM <原事实表> WHERE id=:oid AND is_effective
       FOR UPDATE                                                  -- 行锁抢资格
     → 未命中（已被冲正）: ROLLBACK，返回幂等结果（首次冲正引用），零副作用
  ③ INSERT INTO aud.reversal_record(original_ref_type, original_ref_id, ...)
       （必须先于翻转落库——T-10 校验记录先行）
  ④ UPDATE <原事实表> SET is_effective=false WHERE id=:oid         -- T-10 白名单窄路径
  ⑤ INSERT INTO whs.stock_ledger(..., correction_of_id=:原账行id,
       qty_weight= -原幅值, movement_type=与原行相同)
  ⑥ UPDATE whs.stock_balance SET weight_kg = weight_kg + 原幅值
COMMIT
```

### 2.2 强制规则

| 编号 | 规则 |
|---|---|
| R-RV-1 | ③④⑤⑥ **必须同一事务**；禁止拆成多个事务（如"先插冲正记录提交，稍后翻转"——会在 DB 留下半完成态且不被 T-10 拦截） |
| R-RV-2 | 并发请求**必须只有一个成功**：败者在 ② 的 `FOR UPDATE` 复验中落空，**零副作用返回**（F-4 实测：恰一胜、reversal_record=1、反向账行=1） |
| R-RV-3 | 失败请求不得写入任何行（含 reversal_record） |
| R-RV-4 | **禁止通过普通 UPDATE 绕过冲正流程**：is_effective true→false 必须且只能在上述流程内发生（T-10 强制 GUC + 记录先行；false→true 复活一律被 T-10 阻止） |
| R-RV-5 | 反向账行 `qty_weight` 反号、`correction_of_id` 指向原账行——禁止用 movement_type='return' 冒充冲正 |
| R-RV-6 | 冲正适用于：直接消耗、领料账行、退料、余料入池/出池等一切已落账事实；冲正后如需更正，走**新业务事实**，禁止"冲正再原样重做"以外的手工拼账 |

### 2.3 NB-P3-1 风险记录（当前开放的 NON-BLOCKER）

- `aud.reversal_record` **不存在** `(original_ref_type, original_ref_id)` 唯一约束；
- DB 层无法独立阻止重复冲正记录（F-4b 固化证据：第二条可落库）；
- 当前唯一防线 = 本契约 2.1 的 `FOR UPDATE` 守卫（应用层）；
- mes_app 对 B 类表有 INSERT 权限，NB-1 权限矩阵**拦不住**绕过 service 的直写双冲正；
- 若未来裁决追加 partial unique，属数据库设计变更，须单独走裁决流程；在裁决前，本契约 2.1 为**强制实现标准**。

---

## 三、生产 → 质量状态机契约

### 3.0 总纲

- 数据库 ENUM 仅保证**值合法**（非法值被 CHECK/ENUM 拒绝，d2 已验证）；**状态顺序合法性 100% 属于 service 层强制责任**（当前 DB 允许任意跳值，如 pending→completed、not_started→shipped）；
- service 每次状态变更必须：读当前值 → 校验转换在合法表内 → 同事务 UPDATE 并写对应事实/事件表（履历）→ 不在表内的转换一律业务报错；
- 状态更新与对应事实（如放行=final_qualification）必须同事务。

### 3.1 production_task.status 转换表

| from \ to | pending | in_progress | paused | completed | cancelled |
|---|---|---|---|---|---|
| pending | — | ✅ 开工 | ✅ 暂停 | ⛔ 禁止（未开工不得完成） | ✅ 取消 |
| in_progress | ⛔ | — | ✅ 暂停 | ✅ 完工 | ✅ 取消 |
| paused | ⛔ | ✅ 恢复 | — | ⛔ 禁止跳转 | ✅ 取消 |
| completed | 终态 | ⛔ | ⛔ | — | ⛔（取消事实改走 task_cancellation + need_regen） |
| cancelled | 终态 | ⛔ | ⛔ | ⛔ | — |

- 所有暂停/恢复/取消必须写 `task_event`（B 类，event_type CHECK 五值）；
- 取消必须写 `task_cancellation`（一任务至多一次，UNIQUE task_id）；
- 返工**不改原任务状态**：原任务保持 completed/cancelled，返工走 `rework_order`（源引用 inspection/ncr/engineering_change）→ 新任务 `attempt+1`、`rework_of_task_id` 指原任务（身份三元组唯一保证 attempt 递增合法）。

### 3.2 actual_component.production_status 转换表（enum_ac_production 十值）

```
not_started ──开工──▶ in_production ──暂停/恢复──▶ paused
in_production ──全部工序完工──▶ production_completed
production_completed ──成品上架──▶ in_stock ──装托──▶ on_pallet
   ──装柜──▶ in_container ──发运──▶ shipped ──现场签收──▶ site_received
任意状态 ──报废审批生效──▶ scrapped（终态）
```

| 规则 | 内容 |
|---|---|
| 由生产接口改变的 | not_started / in_production / paused / production_completed |
| 由仓储接口改变的 | in_stock / on_pallet / in_container（与 finished_goods_stock 同事务联动） |
| 由发运接口改变的 | shipped / in_container（与 shipment 事实同事务联动） |
| 由现场接口改变的 | site_received（与 site_delivery_record 同事务） |
| 由报废接口改变的 | scrapped（必须存在生效 `component_scrap_record`，partial unique 保证一补一锚点） |
| 禁止 | 任何跨段跳转（如 not_started→in_stock、in_production→shipped、production_completed→not_started）；scrapped 后一切恢复 |
| 补件 | 报废件不复活；补件=新建 actual_component（seq 不复用），`replacement_for` 指向报废件（T-2 校验指向态必须 scrapped） |

### 3.3 actual_component.quality_status 转换表（enum_ac_quality 六值）

```
pending_inspection ──QC判定──▶ first_pass | failed
failed ──返工指令──▶ reworking ──返工完成重新报验──▶ reinspection
reinspection ──QC复检判定──▶ first_pass | failed
first_pass ──最终放行──▶ final_qualified
```

| 规则 | 内容 |
|---|---|
| 由 QC 接口改变的 | pending_inspection→first_pass/failed；reinspection→first_pass/failed |
| 由生产/返工接口改变的 | failed→reworking；reworking→reinspection |
| 由放行接口改变的 | first_pass→final_qualified（**必须同事务** INSERT final_qualification 事实，partial unique 保证当前放行唯一） |
| 禁止 | pending_inspection 直接跳 final_qualified（未检验放行）；failed 直接跳 final_qualified（不合格放行）；first_pass 未放行前回退 pending_inspection |
| 历史 QC 结果 | `quality_inspection.conclusion` 一次置值后**永久冻结**（T-9）；复检**永远走新 attempt 行**，禁止改旧行——旧 fail 不阻断合法复检（j2 实测） |
| final_qualified 撤销 | 仅走 `final_qualification` 的 revoke 一次性置值（T-16，白名单仅此三列）；**普通接口禁止覆盖放行事实**。**撤销后 quality_status 不回退，保持 final_qualified（裁决 Q1=A）**——构件当前状态口径与放行资格口径分离：一切放行资格判定只准读 `final_qualification` 事实表（存在 is_qualified AND NOT revoked 行），**禁止读 quality_status 判定放行资格**（发运/包装/看板/驾驶舱同此铁律）；撤销后如需再放行，INSERT 新的未撤销 final_qualification 行（partial unique 允许） |

### 3.4 quality_inspection.inspection_status 转换表（enum_inspection 三值）

| from \ to | registered | inspecting | concluded |
|---|---|---|---|
| registered | — | ✅ 开始检验 | ⛔ 禁止（未检验出结论） |
| inspecting | ⛔ | — | ✅ 出结论 |
| concluded | 终态（行冻结） | ⛔ | — |

- `conclusion`（pass/fail）仅允许在 status=concluded 的事务内一次置值（T-9 白名单 {conclusion, inspection_status}）；
- conclusion 已置值后**任何** UPDATE 均被 T-9 拒绝（含"同值重写"以外的一切变更）。

### 3.5 放行独立性（J-1 契约化）

- 生产完成（task.completed + production_status=production_completed）**≠** 质量放行；
- 放行唯一表达 = `final_qualification` 事实行（is_qualified, NOT revoked）；
- **放行前置条件（service 强制校验，DB 无此约束）**：该构件存在 conclusion='pass' 的检验行，且**当前**放行事实（is_qualified AND NOT revoked 的 final_qualification 行）不存在，且构件未处于 reworking/failed；
- 放行资格的**一切下游判定**（发运 releasable 硬校验 RED-09、包装/装托/装柜、驾驶舱"最终合格"口径）**只准以 final_qualification 事实表为准**（Q1=A），禁止读 quality_status 替代；
- 数据库当前**不校验**上述前置（INSERT final_qualification 仅受 partial unique 约束）——service 必须实现，违者视为契约违规。

---

## 四、库存幂等契约

### 4.1 幂等方式总表（逐动作，拒绝笼统）

| 动作 | 幂等方式 | DB 既有锚点 | service 判据 |
|---|---|---|---|
| 生产报工 | action_key（业务动作幂等）+ client_token（网络幂等） | `uq_production_report_task_action_key`、`ix_production_report_client_token` | 先查 (task_id, action_key) 命中→返回首次报工；report_seq 仅显示序号**非**幂等键（NB-3 三者分工） |
| 直接消耗 | client_token | `ix_material_consumption_client_token`（partial unique） | 先查 token 命中→返回首次消耗事实 |
| 领料/出库 | 单据号=**客户端生成幂等令牌**（Q4=A） | `uq_material_issue_document_issue_no` + `uq_material_issue_line_doc_line_no` | 重试必须携带同一 issue_no；service 先查命中→返回首次单据+过账结果 |
| 预留 | 单据号=**客户端生成幂等令牌**（Q4=A） | `uq_material_reservation_reservation_no` | 同上，重试携带同一 reservation_no |
| 构件报废 | scrap_no=**客户端生成幂等令牌**（Q4=A）+ 生效唯一 | `uq_component_scrap_record_scrap_no`、`ix_component_scrap_record_effective` | 先查构件生效报废命中→返回首次报废 |
| 入库（成品上架） | 业务唯一锚 | `uq_finished_goods_stock_in_stock`（在库 partial unique） | 先查构件在库行命中→返回首次上架 |
| 入库（材料上架）/退料/余料入池出池 | **client_token（Q3=A）** | `stock_ledger.client_token`（partial unique WHERE NOT NULL，**由迁移 M-P4-1 落地**——已裁决的唯一结构变更，见第七章） | service 先 SELECT by client_token 命中→返回首次账行；分批上架/多次退料/一行多块余料为合法业务，禁止以业务引用判重 |
| 材料/余料报废 | scrap_no=**客户端生成幂等令牌**（Q4=A）+ 账行 client_token | scrap_no（客户端生成）+ `stock_ledger.client_token`（M-P4-1） | 双锚点：先查 scrap_no 命中→返回首次报废；出池账行受 token 保护 |
| 冲正 | 状态守卫 | （NB-P3-1：无 DB 唯一键） | 第二章 2.1 守卫；已冲正→返回首次冲正引用 |

### 4.2 幂等返回语义（强制）

- R-ID-1：同一请求发送两次：第一次成功，第二次**必须返回第一次操作的确定性结果**（同一业务事实 id / 同一单据号 / 同一冲正引用 + 明确幂等标识），**绝不产生第二笔业务事实、第二条账行、第二行余额变动**；
- R-ID-2：幂等判据查询与写入必须同事务（或先查后写+唯一键兜底），禁止"查完再插"之间的竞态窗口无兜底；
- R-ID-3：**UniqueViolation 不得作为正常业务幂等设计**——它只能作为并发竞态的**兜底防线**：捕获后必须**反查既有结果并按 R-ID-1 返回**，而不是把数据库报错直接抛给用户；
- R-ID-4：不同业务动作禁止共享幂等键空间（action_key 限报工、client_token 限其所在表的既有列，冻结设计未授权扩散）；
- R-ID-5：**幂等是服务端契约，与终端形态无关**（裁决 Q3 重新明确）——库存 Service/API 无论由 PC、Android 还是任何其他客户端调用，重复请求一律按本章语义处理；不得因某动作"只在 PC 操作"而跳过幂等实现。

---

## 五、Phase 4 实现边界

### 5.1 数据库负责（Phase 3 已交付，冻结）

| 层 | 内容 |
|---|---|
| 结构 | 131 表 / 8 schema；PK 全部 IDENTITY |
| 引用 | 216 FK 全 RESTRICT（0 CASCADE） |
| 唯一 | 89 UNIQUE + partial unique（放行当前唯一/在库唯一/补件一补一/client_token/QR 1:1） |
| 值域 | 22 封闭 ENUM + 71 CHECK（负库存底线、qty≠0、surplus_id 强制、批次入口三态等） |
| 触发器 | T-1~T-16（快照冻结/血缘指向态/模板行锁定/make_type 与 material_code 终局/导入行冻结/质检结论一次置值/消耗冲正窄路径/领料聚合/清单 void/单据状态窄路径/验收窄路径/图纸与放行窄路径） |
| 生命周期 | NB-1 权限矩阵：mes_app SELECT+INSERT 全表、UPDATE 仅 81 表（A/C/白名单 B）、DELETE=0 |
| 不可逆/不可覆盖 | 结论一次置值、revoked 一次性、voided 单向、冲正不可复活、历史事实无 DELETE |

### 5.2 Service/API 负责（Phase 4 交付，本文为验收依据）

| 层 | 内容 |
|---|---|
| 事务编排 | 第一章七类动作统一模板（①锁②事实③账④余额，同事务，整体回滚） |
| 状态机 | 第三章四条转换表 + 变更接口归属 + 转换与事实同事务 |
| 幂等入口 | 第四章逐动作幂等方式 + R-ID-1~4 返回语义 |
| 冲正守卫 | 第二章 F-4 模式四步同事务 + NB-P3-1 期间为唯一防线 |
| 原子流程 | 库存七类动作完整实现（禁止跨事务拆分） |
| 资格 | 登录态、角色/权限（DB 权限矩阵是**最后防线**，不是业务鉴权）、审批留痕（approved_by/approved_at 填充） |
| 错误码 | 业务错误码体系 + 幂等返回 + R-ID-3 的 UniqueViolation 转换 |

### 5.3 禁止事项

- D-1：Phase 4 **不得通过修改数据库结构**（加列/改约束/改触发器/改迁移）来"重新解释"V1.2 已冻结业务模型；发现设计不足走 GAP 记录 + 单独裁决；
- D-2：不得修改已作为基线的 Migration（`0b4e2935fee4`→`a7c1d2e4b601`→`d8f2a4c6e810`→`e9a3b5c7d921`）；
- D-3：不得以 ORM 层默认值/钩子替代本文 service 校验（状态机/幂等/守卫必须在显式 service 代码中可审计）；
- D-4：不得在未实现第二章守卫前开放任何冲正接口；
- D-5：不得绕过 T-9/T-10/T-11/T-16 窄路径（含在 ORM 中对白名单外列发起 UPDATE）。

---

## 六、自检结论

### 6.1 与 V1.2 Domain Model 冲突检查

- **V1.0/V1.1**：零新增实体/业务状态/业务关系/约束；全部引用均来自冻结设计（130 实体/102 关系/34 约束清单内）；
- 3.1~3.4 转换表仅是 ENUM 值域内的顺序合法化细化，未改变任何值域与封闭集合；
- 第四章幂等方式全部落在**已存在的** DB 锚点上（client_token/issue_no/reservation_no/scrap_no/action_key/in_stock partial unique），未新增列或索引；
- **V1.2（2026-09-19 发运域裁决）**：新增 `ship.shipment_pallet` 实体（2 条 partial unique）与 `ship.shipment.container_id` 列（nullable，无唯一）——系业务方明确裁决的发运操作单位承载实体（红队审查 HIGH-2 断点修复），经批准走迁移 **M-P4-2**；`ship.container_seal` **不实施**（B-0 关闭）；发运域其余判定全部复用既有事实表（pallet_load/container_load/shipment_snapshot），不新增业务状态值；
- **结论：无冲突（V1.2 新增项均经显式裁决并记录于 7.3）。**

### 6.2 与 Phase 3 实际数据库实现冲突检查

逐锚点核对（均有测试/迁移证据）：`(task_id, action_key)` UNIQUE（c2）、client_token 两处 partial unique（c3/f2）、issue_no/reservation_no/scrap_no UNIQUE、`uq_finished_goods_stock_in_stock`、`uq_material_issue_line_doc_line_no`、T-9/T-10/T-11/T-16 行为（t9/t10/t11/t15_t16）、负库存 CHECK（f3）、T-10 记录先行与不可复活（t10/f4）、NB-P3-1 无唯一键（f4b）——与本文描述一致；
- **待裁决项处理结果（2026-09-19 Gate 裁决，全部落定）**：
  - **FQ-1 → 裁决 Q1=A**：revoke 后 quality_status 不回退；放行资格判定只读 final_qualification 事实表（已修订 3.3/3.5）；
  - **SC-1 → 裁决 Q2=A**：链路完整、无账务守恒断点；**五账对账（领料−消耗−退料−余料−报废，按批次与按班组口径）定为 Phase 4 强制交付**（查询级对账视图，不新增实体字段，在制差额可见但不强制清零）；
  - **ID-1 → 裁决 Q3=A + Q4=A**：`stock_ledger` 增加 `client_token`（partial unique，迁移 **M-P4-1**，已批准的唯一结构变更）；领料/预留/报废单据号改为客户端生成幂等令牌（已修订第一章/4.1）；
  - **GAP-4 → 已勘误**：契约 1.1 原判据（"非零在库即拒绝重复上架"）与分批上架合法业务冲突，V1.1 已修订；
  - **W-5 批次直接报废路径**：暂按一代余料承载实现，若 Phase 4 实测体验不可接受再走 GAP 裁决。
- **结论：无冲突。**

### 6.3 总判定

**Phase 3 数据库与 Phase 4 应用层契约边界一致。**

> 本文档仅固化边界与责任，未修改数据库结构、Migration、Trigger、ORM、Domain Model，未创建任何 Phase 4 代码。

---

## 七、终端使用边界与 Phase 4 前置交付清单（2026-09-19 裁决新增）

### 7.1 终端使用边界（业务范围裁决，冻结）

**Android 手机端只承载以下四类车间现场业务**（扫码 + 4G/5G 场景）：

| # | 业务 | 核心交互 | 网络风险重点 |
|---|---|---|---|
| 1 | 构件登记 | 扫码（QR）、补件登记 | 重复点击、超时重试 |
| 2 | 报验 | 扫码 → 提交检验申请 | 超时重试、并发提交 |
| 3 | 发运 | 扫码 → 装柜/发运确认 | 超时重试、releasable 硬校验（RED-09，只读 final_qualification 事实表） |
| 4 | 设备报修 | 扫码/选择设备 → 报修单 | 重复提交 |

**其余业务一律默认 PC 管理端承担**（不限于）：项目管理、构件清单、生产计划、班组任务分配、物料入库、领料/出库、退料、余料管理、材料报废及其他办公室管理业务。

**强制边界（R-DEV-1~3）**：

- R-DEV-1：Android 功能范围**冻结为上述四类**；禁止自行增加 Android 物料入库、领料、退料、余料、材料报废、项目管理、生产计划、班组任务配置（如未来需要，走单独裁决，默认拒绝）；
- R-DEV-2：**幂等不因终端而豁免**（R-ID-5 同源裁决）：Q3/Q4 的网络级幂等是因为"库存 Service/API 无论由 PC、Android 或其他客户端调用，都不能允许重复请求造成重复记账"，**不是因为所有库存业务都要在手机端操作**；PC 端调用的库存接口同样必须完整实现第一章/第四章契约；
- R-DEV-3：四类 Android 业务在 4G/5G 弱网下的实现要求：客户端生成幂等令牌（Q4=A）、请求重试携带同一令牌、服务端幂等返回（R-ID-1~3）、重复点击客户端侧防抖仅为体验优化**不构成**幂等机制。

### 7.2 Phase 4 开工前置交付清单（已裁决待执行）

| 项 | 内容 | 性质 |
|---|---|---|
| **M-P4-1** | Alembic 迁移：`whs.stock_ledger` 增加 `client_token` 列（String(64) NULL）+ partial unique（WHERE NOT NULL）+ 查询索引 | **已批准的唯一结构变更**（裁决 Q3=A）；Phase 4 首个迁移，先行执行；不触碰 131 表其余结构与 T-1~T-16 |
| D-P4-1 | 五账对账视图（领料−消耗−退料−余料−报废，按批次/按班组口径，查询级） | Phase 4 强制交付（裁决 Q2=A） |
| C-P4-1 | Android 功能范围冻结清单（7.1） | Phase 4 Android 开发唯一依据（R-DEV-1） |
| C-P4-2 | 客户端单据号生成规范（issue_no/reservation_no/scrap_no 幂等令牌式，Q4=A） | Phase 4 API 契约组成部分 |
| **M-P4-2** | Alembic 迁移：新表 `ship.shipment_pallet`（含 2 条 partial unique）+ `ship.shipment` 加列 `container_id`（nullable FK，无唯一约束） | **已批准的结构变更**（Q5/Q7 裁决，V1.2 唯一例外新增实体）；**设计文档** `docs/v1.2_mp42_migration_design.md`（只设计，执行在发运 Sprint 开工时）；执行顺序 M-P4-1 → M-P4-2；`ship.container_seal` **不实施**（Q6） |
| **D-P4-2** | 发运清单三态视图（未装托/打包/已发货，全部事实表推导）+ shipped 缓存位与发运事实差异对账查询 | Phase 4 强制交付（第八章 8.1/8.2） |
| **S-P4-1** | 发运域测试组（9 条测试矩阵，第八章 8.10） | 发运 Sprint 开工强制交付（与实现同批，不可后补） |

### 7.3 裁决记录（2026-09-19）

| 决策 | 结论 |
|---|---|
| Q1（FQ-1 revoke 后状态） | **A** — 不回退；放行资格只读事实表 |
| Q2（SC-1 五账对账） | **A** — 强制交付，在制差额可见不强制清零 |
| Q3（无键账务动作幂等） | **A** — stock_ledger 加 client_token（M-P4-1）；幂等理由为服务端普适契约，与终端无关 |
| Q4（单据号生成策略） | **A** — 客户端生成单据号（幂等令牌式） |
| Q5（发运操作粒度与载体） | **A** — 托盘为发运操作单位；新增 `ship.shipment_pallet` 正式实体（红队 HIGH-2 修复）；发运清单不建业务表，三态由事实实时推导 |
| Q6（封条） | **不实施** — `container_seal` 第一阶段不建（红队 B-0 关闭）；不因封条增加任何发运前置条件 |
| Q7（shipment.container_id） | **nullable** — 兼容散运/裸件发运；一箱可关联多个 shipment，不建唯一约束 |
| Q8（sealed→loading） | **允许重开** — 必须经 unload/reload 正式动作并保留完整历史与操作人，禁止直接改状态 |
| Q9（发运质量前置） | **仅 RED-09**（final_qualification 事实表）；暂不以 production_completed/painting 作发运前置（E-2 口径不变：painting 完成驱动 production_completed） |

---

## 八、发运域契约（2026-09-19 裁决冻结，V1.2 新增）

> 业务背景与裁决过程见 `docs/v1.2_shipping_business_decisions.md`；迁移设计见 `docs/v1.2_mp42_migration_design.md`。
> 本章规则编号 R-SH-*，与 R-TX/R-RV/R-ID/R-DEV 同级效力。

### 8.0 发运域总纲（R-SH-0）

1. **三层事实分离**：装载事实（`pallet_load`/`container_load`，is_current 当前态）× 操作事实（`shipment_pallet`，托盘级发运归属）× 归属快照（`shipment_snapshot`，确认时刻构件级冻结，T-1 保护）。
2. **缓存位铁律（对应裁决 F）**：`actual_component.production_status` 的 `on_pallet`/`in_container`/`shipped`（及 `site_received`）**仅是缓存展示位**，由对应事实事务内同步写入；真实业务判定一律以 pallet_load、container_load、shipment_pallet、shipment_snapshot、shipment 事实为准，**禁止**读缓存位作业务判定。
3. **不建立"发运清单"业务表（对应裁决 A）**：发运清单是事实表实时推导的查询（视图 D-P4-2），禁止新建任何清单/缓存表。
4. 发运域**不新增任何业务状态值**：shipment/container/pallet/production_status 全部沿用既有 ENUM 值域；本章仅固化值域内的顺序合法化与判定规则。

### 8.1 发运清单三态（R-SH-1，对应裁决 C/E）

| 态 | 唯一判定条件（事实源） |
|---|---|
| 未装托 | 构件存在（非 scrapped）且 `production_status ∈ ('production_completed','in_stock')` 且**不存在** `pallet_load.is_current=true` 行 |
| 打包 | 存在 `pallet_load.is_current=true` 行（含托盘已进箱/封柜——构件仍在托上） |
| 已发货 | 构件当前托盘存在 `shipment_pallet.is_current=true` 且关联 `shipment.status ∈ ('confirmed','in_transit','delivered')` |

- 铁律：三态判定**全部读事实表**，禁止以 `production_status='shipped'` 判定已发货（与 Q1=A 放行判定同款铁律）；
- 展示层扩展态：现场签收后可显示"已签收"（`site_delivery_record` 推导），仅为展示，不新增业务态；
- `scrapped` 构件不出现在发运清单；
- 视图 D-P4-2 交付上述三态查询 + shipped 缓存位与事实差异对账。

### 8.2 shipment_pallet 正式实体（R-SH-2，对应裁决 B/C/D）

- 托盘是发运操作单位；`ship.shipment_pallet` 记录 shipment ↔ pallet 归属（迁移 M-P4-2）；
- **约束①** `uq_shipment_pallet_current_pallet`：partial unique `(pallet_id) WHERE is_current=true` → 一个托盘同一时间只能属于一个有效 shipment（草稿或已确认），**DB 硬拦**（对应裁决 C）；
- **约束②** `uq_shipment_pallet_current`：partial unique `(shipment_id, pallet_id) WHERE is_current=true` → 同单不重复加托；
- draft 阶段移出草稿 = `is_current=false`（NB-1 权限矩阵 DELETE=0，禁止 DELETE，历史行保留）；
- **已确认 shipment 的 shipment_pallet 不允许解除**（对应裁决 D）：确认后行冻结（is_current 永久 true）→ 已发货托盘再次加入任何发运单被约束①硬拦；
- 加托入草稿前置校验（service）：托盘当前在箱（`container_load.is_current=true`）且该箱 = `shipment.container_id`；若 shipment.container_id 为 NULL（散运），放行该前置（散运允许托盘不进箱直接入单）。

### 8.3 shipment.container_id 与混托归属（R-SH-3，对应裁决 L）

- `container_id` **nullable**（散运/裸件发运合法）；**无唯一约束**（一箱可关联多个 shipment）；
- **混托归属规则**：`shipment_pallet` 是操作事实（整托随单走）；`shipment_snapshot` 是归属事实（构件级，可跨主项目）；`shipment.main_project_id` 仅表示发运单主项目，**混托构件的发运归属一律以 snapshot 为准**，构件反查链（构件→托盘→箱→发运记录）不受 main_project_id 影响；
- 跨主项目混托仍须 `mixed_load_authorization`（既有约束 9，不变）。

### 8.4 发运确认原子事务（R-SH-4，对应裁决 G/H/I）

发运确认 = **单事务五步**，任何一步失败整体回滚（shipment 保持 draft）：

```
① shipment: draft→confirmed + confirmed_at/confirmed_by（状态守卫：非 draft 一律拒绝，重复确认按幂等返回首次结果）
② 关联 shipment_pallet 全部行冻结
③ 逐构件 INSERT shipment_snapshot（圈定 = pallet_load.is_current 集合；快照即冻结，T-1 保护）
④ 逐构件 UPDATE production_status='shipped'（缓存位，同事务）
⑤ finished_goods_stock 联动 status='out'（如存在在库行）
```

- **确认前置校验（R-SH-5，对应裁决 H）**：单内全部构件必须存在有效放行事实（`final_qualification.is_qualified AND NOT revoked`，RED-09，只读事实表）；
- **明确不校验** production_completed/painting（对应裁决 I）；production_completed 仍按 E-2 冻结口径由 painting 完成驱动，与发运域无联动；
- 快照行数必须 = pallet_load is_current 行数（同事务校验），保证整托原子（"要么整托成功，要么整个确认失败"）。

### 8.5 已发货判定与退运释放（R-SH-6，对应裁决 E/J）

- **已发货唯一依据** = `shipment_pallet.is_current=true` + `shipment.status >= confirmed`（R-SH-1 表第三态）；
- **退运**：仅经 `shipment_event(event_type='return')` 正式事件 → 受控释放流程：shipment_pallet `is_current=false`（保留历史行）+ `pallet_load_history` 写 remove 事件 + 构件缓存位由释放事务内同步回退（shipped→in_stock）+ `pallet.status` 回 empty（托盘复用）+ `finished_goods_stock` 回 `in_stock`（如确认时曾联动置 out，同事务回补，注意在库 partial unique 锚）；
- **禁止**任何绕过退运事件的直接改状态（构件/托盘/箱状态直改均属契约违规；service 层不得暴露直改 API）；
- 退运后复用：构件重新装托须重新走装托校验；补发 = 新 shipment 行（ShipmentEvent 'reship' 既有语义）。

### 8.6 sealed→loading 重开（R-SH-7，对应裁决 K）

- 允许重开（海关查验等场景）；
- **必须**通过正式动作：`container_load_history` 写 `unload` 事件（含 operator）→ container.status sealed→loading → 重装 → `reload` 事件 → 再封柜（sealed）；**禁止**直接 UPDATE container.status；
- 卸柜前必须先处理柜内托盘（托盘 unload → 才可拆托，顺序由 service 强制）；
- 全程历史与操作人完整留存（container_load_history.operator 必填于 unload/reload 动作）；
- DB 层加固（状态变更须伴随历史行同事务，GUC 上下文模式）列为**建议项**，待单独裁决后实施（见迁移设计文档）。

### 8.7 封条（R-SH-8，对应裁决 M/Q6）

- `ship.container_seal` **第一阶段完全不实施**，不建表、不加列；
- 不因封条增加任何发运前置条件；`container.status='sealed'` 语义保留（封柜状态），不记录封条号；
- 未来若需记录，走独立裁决 + 新迁移，本契约届时修订。

### 8.8 发运域并发与幂等（R-SH-9，对应裁决 N）

| 动作 | 幂等方式 | DB 锚点 | service 判据 |
|---|---|---|---|
| 装托 | 先查后写 + 唯一锚兜底 | `uq_pallet_load_current_component` / `uq_pallet_load_current`（partial unique，已有） | 先查构件 is_current 命中→返回首次装托结果；UniqueViolation 按 R-ID-3 反查转换 |
| 加托入草稿 | 先查后写 + 唯一锚兜底 | `uq_shipment_pallet_current_pallet` / `uq_shipment_pallet_current`（M-P4-2） | 先查托盘 is_current 命中→返回首次归属；已在其他草稿→明确业务错误 |
| 移出草稿 | 状态守卫 | is_current 状态位 | 非 draft 单的托盘禁止移出 |
| 发运确认 | 状态守卫 + shipment_no 幂等 | `uq_shipment_shipment_no` + draft 状态守卫 | 非 draft → 拒绝或按幂等返回首次确认结果 |
| sealed 重开（unload/reload） | 事件守卫 | container_load_history append-only | 非 sealed 状态禁止 unload；事件与状态同事务 |

- 幂等返回语义沿用 4.2（R-ID-1~5）；装托/加托/确认接口由 PC 端调用，R-ID-5 同样适用。

### 8.9 发运域状态机转换表（R-SH-10）

**shipment.status**（enum_shipment 五值）：

| from \ to | draft | confirmed | in_transit | delivered | returned |
|---|---|---|---|---|---|
| draft | — | ✅ 确认（R-SH-4 五步） | ⛔ | ⛔ | ⛔ |
| confirmed | ⛔ | — | ✅ 发车/离港（depart 事件） | ⛔ | ✅ 退运（return 事件 + R-SH-6 释放流程） |
| in_transit | ⛔ | ⛔ | — | ✅ 签收（sign 事件） | ✅ 退运 |
| delivered | ⛔ | ⛔ | ⛔ | — | ✅ 退运 |
| returned | ⛔（补发=新 shipment 行） | ⛔ | ⛔ | ⛔ | — |

**shipping_container.status**（enum_container 五值）：

```
empty → loading → sealed → shipped → returned → empty（复用）
sealed → loading 允许（R-SH-7：必须 unload/reload 正式动作，禁止直接改状态）
loading → empty 允许（柜内托盘全部 unload 后）
```

- 推进归属：`shipped` 由关联 shipment 的离港事务（depart 事件，shipment.status confirmed→in_transit）同事务驱动；`returned` 由退运释放事务（R-SH-6）驱动——container.status 同样是跟随事实的缓存位（R-SH-0.2），禁止脱离 shipment/container_load_history 事实单独推进。

**pallet.status**（enum_pallet 四值）：`empty → in_use → loaded → in_container`；`in_container → empty` 仅经退运释放流程（R-SH-6）。

**构件物流段缓存位**（enum_ac_production，属 3.2 转换表的物流段）：`in_stock → on_pallet → in_container → shipped → site_received`，均由对应事实事务内同步写（缓存位铁律 R-SH-0.2）；`scrapped` 终态不受影响。

### 8.10 发运域测试矩阵（R-SH-11，交付项 S-P4-1）

| # | 测试 | 验证目标 |
|---|---|---|
| 1 | 同一构件并发进入两个托盘 | `uq_pallet_load_current_component` 硬拦 + 幂等转换 |
| 2 | 同一托盘并发加入两个 shipment | `uq_shipment_pallet_current_pallet` 硬拦 |
| 3 | 已确认托盘再次加入 shipment | 约束① 行冻结后硬拦 |
| 4 | shipment 确认过程 snapshot 生成失败 | 五步整体回滚，shipment 保持 draft，无残留构件状态/快照 |
| 5 | 已发货构件再次装托 | service 事实表校验拒绝（退运释放前） |
| 6 | sealed → unload → loading → reload → sealed | R-SH-7 动作链 + 历史完整（unload/reload 行 + operator） |
| 7 | 发运三态推导正确性 | D-P4-2 视图：未装托/打包/已发货与事实一致（含缓存位漂移暴露） |
| 8 | container → pallet → component 反查 | 柜号/柜型/状态/托盘/构件全链可达 |
| 9 | component → pallet → container → shipment 反查 | 构件→项目/子项目/构件号/托盘/柜号/柜型/发运记录全链可达 |

补充：确认前置校验测试（含 final_qualification 撤销后禁止发运）并入现有放行测试组；混托授权测试沿用既有约束 9 场景。

### 8.11 发运域服务层禁止事项（R-SH-12）

- D-SH-1：禁止建立任何"发运清单"业务表或构件级物流状态人工维护接口；
- D-SH-2：禁止在 R-SH-4 五步事务之外生成 shipment_snapshot 或写构件 shipped 缓存位；
- D-SH-3：禁止绕过 shipment_event('return') 直接回退构件/托盘/箱状态；
- D-SH-4：禁止以 production_status 任何物流段值作为业务判定依据（仅限展示）；
- D-SH-5：禁止对已 confirmed 的 shipment 主行做 status 之外的任何 UPDATE（行冻结；DB 触发器加固待裁决，service 为当前唯一防线）。
